# Risk Register — 2026-06-12 (Sprint 16 sprint-review 이연 findings)

> 출처: sprint-review 코드리뷰 Plausible/Low 이연 항목 + 전체 코드리뷰 P2 미진행/P3 연계
> 등록일: 2026-06-12

---

## sprint-review 이연 Findings

| ID | 이슈 | 등급 | 출처 | 대응 계획 |
|----|------|------|------|-----------|
| R117 | `MoveAttendanceDialog.handleSelect` — `finally` 블록 없어 `void invalidateQueries` throw 시 `submitting=true` 잔존, 달력 전체 영구 비활성 | Medium | sprint-review FE-A1 (CONFIRMED) | `setSubmitting(false)`를 finally 블록으로 이동 (A105). 차기 안정화 스프린트 |
| R118 | `notices/page.tsx` 구형 guard 패턴 — `setUnsavedNavTarget` 미호출로 시스템 공통 `UnsavedNavDialog`와 아키텍처 불일치. 현재는 자체 pendingAction 모달로 커버 | Medium | sprint-review FE-B4 (CONFIRMED, 기능적 동작) | P2-4 notices 분리 스프린트에서 `useUnsavedChanges` 훅으로 마이그레이션 (A104) |
| R119 | `move_attendance_impl` status 체크 ~ UPDATE 사이 트랜잭션 미보호 (TOCTOU) — 단독 운영 앱 특성상 실현 가능성 낮으나 구조적 취약점 | Low | sprint-review B5 (PLAUSIBLE, 실현 가능성 낮음) | 차기 attendance 도메인 리팩토링 시 명시적 트랜잭션으로 묶기 |
| R120 | `apply_schedule_change_impl` set_schedule 선행 완료 여부 미검증 — 현재 호출경로(schedule-editor.tsx)는 항상 await set_schedule 선행 보장. 미래 호출경로 추가 시 위험 | Low | sprint-review Rust-A4 (PLAUSIBLE, 현재 재현 불가) | apply_schedule_change_impl에 effective_from의 student_schedules 존재 여부 DB 검증 추가 검토 |
| R121 | `compute_summary`와 `fetch_pending_absences` 동일 WHERE 술어 SQL 중복 — 조건 변경 시 정합 불일치 + get_grid_impl 내 N+1(학생당 2쿼리) | Low | sprint-review R6·E2 (P2-1 연계) | P2-1 출결 그리드 N+1 최적화 스프린트에서 통합 쿼리로 교체 (A106) |
| R122 | `weekday_ko()` 함수 `attendance.rs`·`notice.rs` 이중 정의 — 요일 표기 변경 시 한 쪽 누락 시 수업일 이동 메모와 공지문 표기 불일치 | Low | sprint-review weekday_ko 중복 (R1) | P2-10 백엔드 위생 스프린트에서 `util_date.rs` 공유 모듈로 추출 (full-review P2-10 연계) |

---

## cipher 실동작 미검증 리스크

| ID | 이슈 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R123 | cipher 기능(백업/복원·무결성·DB폴더변경) dev 환경 stub — 첫 실검증이 v1.0.0 인스톨러(.exe) 배포 산출물에서 일어남 | 높음 | sprint-review 자동검증 한계, sprint16 계획 R110 연계 | deploy-prod 완료 직후 A101 스모크 테스트 수행. 실패 시 즉시 hotfix |

---

## 전체 코드리뷰 P2·P3 중기 리스크 (요약 연계)

> 상세: `docs/code-review/full-review-2026-06.md` 섹션 ④·⑤
> 아래는 v1.0 이후 스프린트 계획 시 참조할 주요 항목만 발췌.

| 참조 ID | 이슈 요약 | 우선순위 | 권장 스프린트 |
|---------|-----------|----------|--------------|
| P2-1 | 출결 그리드 N+1 (50명 ~350쿼리) + 전체 재렌더 | High | 안정화 1차 |
| P2-2 | 에러 한글화 미통일 121곳 raw sqlx 노출 | High | 안정화 1차 |
| P2-8 | Undo 확대(청구·보강) + 저장 성공 피드백 통일 | High | 안정화 1차 |
| P2-3 | `tauri/index.ts` 1,890줄 비대화 + as 단언 113개 | Medium | 안정화 2차 |
| P2-4 | `notices/page.tsx` ~2,000줄 분리 (R118 포함) | Medium | 안정화 2차 |
| P2-6 | ConfirmDialog 공통화 (인라인 모달 12벌) | Medium | 안정화 2차 |
| P2-10 | 백엔드 위생 — 날짜 유틸 중복, async IO, billing 집계 테스트 부재 | Medium | 안정화 2차 |
| P3 | DB 읽기 풀 분리, dead_code 정리, F1 도움말 등 | Low | 실사용 피드백 후 |
