# Risk Register — 2026-05-18

> Sprint 1 (데이터 인프라) 진입 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R1 | SQLCipher `bundled-sqlcipher-vendored-openssl` 양 OS 빌드 실패 — sqlx와 libsqlite3-sys 통합 시 컴파일 오류 또는 런타임 DB 열기 실패 | 높음 | sprint-planner, PO-review | 첫 2일 PoC에서 양 OS 검증. 실패 시 시스템 sqlcipher 대체 → 2차 실패 시 Sprint 재계획 |
| R2 | `rusqlite` + `sqlx` 동시 사용 시 DB 커넥션 충돌 — 동일 SQLCipher DB에 두 라이브러리가 동시 접근할 때 락 충돌 | 중간 | sprint-planner | rusqlite 커넥션은 백업 전용 읽기 전용으로 제한. sqlx pool과 격리된 별도 경로로 open |
| R3 | 앱 시작 시퀀스 3초 초과 — PBKDF2 600K iterations + 무결성 검증 + 락 확인 합산 시 성능 예산 초과 | 높음 | sprint-planner, 성능엔지니어-review | quick_check (~50ms) + tokio::join! 병렬화로 ~1초 목표. 동기화 대기는 3초 예산 외 별도 처리 |
| R4 | CI (.github/workflows/) Forbidden Area 수정 필요 — SQLCipher 빌드를 위해 CI 파이프라인 수정이 불가피할 수 있음 | 중간 | sprint-planner, PO-review | Forbidden Area 규칙에 따라 사용자 허가 요청 후 진행. vendored-openssl feature로 별도 설치 최소화 |
| R5 | Argon2id 연산 시간 양 OS 편차 — macOS ARM vs Windows x64에서 동일 파라미터 시 소요 시간 차이 | 중간 | sprint-planner, 보안전문가-review | PoC 단계에서 양 OS 벤치마크. 200ms 이내 달성 가능한 파라미터 선정 |
