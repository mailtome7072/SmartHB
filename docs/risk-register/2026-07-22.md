# Risk Register -- 2026-07-22 (Sprint 23 계획, ADR-012 A안 확정 후 개정)

> 출처: sprint-planner
> 등록일: 2026-07-22
> 개정일: 2026-07-23 (ADR-012 A안 확정에 따른 리스크 재평가)

---

## Sprint 23 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| ~~R142~~ | ~~A1 데이터 위치 이전 시 기존 사용자 이관 실패~~ | -- | -- | ADR-012 A안(데이터 이전 없음)으로 **폐기** |
| R143 | after_connect 훅에서 PRAGMA key 적용 시 sqlx 커넥션 풀 동작과의 상호작용 -- idle timeout, max_connections 설정에 따라 새 커넥션에서 PRAGMA 적용 실패 가능 | 중간 | sprint-planner | T1에서 인메모리 테스트 + dev 환경 실 DB 테스트. sqlx 0.8 after_connect 공식 문서 참조. 실패 시 에러 전파하여 풀에 반환하지 않음 |
| ~~R144~~ | ~~sqlite-migration-fk-rebuild 함정 -- T6 이전 시 FK 무결성 깨짐~~ | -- | -- | 데이터 이전 없으므로 **폐기** |
| R145 | ntfs-power-loss-pattern 함정 -- T3/T4 파일 조작 시 fs::write 후 데이터 NULL 손상. 기존 사용자 메모리 기록: atomic write 헬퍼에 손상 감지+fallback 필수 | 중간 | sprint-planner (메모리 참조) | 모든 파일 조작에 Sprint 17 T2에서 도입한 atomic write(tmp+rename) 패턴 적용. 복원 시에도 동일 패턴 |
| R146 | cipher-test-gate-trap -- cipher-on 테스트 시 Windows Strawberry Perl 의존 + 테스트 모듈 게이트 설정 필요. 기존 사용자 메모리 기록 | 중간 | sprint-planner (메모리 참조) | T9에서 `cargo test --features cipher` 통합 수행. CI는 이미 cipher 빌드 포함. 신규 테스트에 `#[cfg(all(test, not(feature="cipher")))]` 게이트 필요 여부 확인 |
| R147 | keyring-v3-features-trap -- T7 키체인 저장 시 features 누락으로 silent set fail 가능. 기존 사용자 메모리 기록 | 중간 | sprint-planner (메모리 참조) | 기존 Cargo.toml의 keyring 설정 확인(`features = ["apple-native", "windows-native"]`). 변경 없으면 리스크 낮음 |
| R148 | T2 빈 DB 판정 강화로 최초 설정(마법사) 흐름에서 오탐 가능 -- 시드만 있는 신규 DB를 빈 DB로 잘못 판정하면 마법사 완주 불가 | 중간 | sprint-planner | 가드 조건에 "salt.bin 존재" 전제를 포함하여 최초 설정 경로를 분리. 마법사 흐름 단위 테스트 |
| R149 | (신규) T6 유휴 close/재연결 시 IPC 응답 지연 -- 유휴 후 첫 IPC 호출 시 풀 재생성 + PRAGMA 적용에 1-2초 소요 가능. 사용자가 "느려짐"으로 체감 | 중간 | sprint-planner | 재연결 시 splash/spinner 표시 검토. WAL 체크포인트는 DB 크기 1MB 미만이라 수백ms 이내 예상 |
| R150 | (신규) 클라우드-라이브-DB 잔여 리스크 -- ADR-012 A안은 안티패턴을 완전 제거하지 않음. 강화 후에도 활성 사용 중 torn-sync 잔여 가능 | 중간 | sprint-planner | A2(생성 가드) + A4(복원 강화) + A5(백업 보호)로 데이터 손실이 아닌 안전 실패로 전환. ROADMAP에 B안 승격 트리거 등록 |
