# Risk Register — 2026-06-02 (Sprint 12 코드 리뷰)

> 출처: sprint-review 코드 리뷰 (F1)
> 등록일: 2026-06-02

---

## Sprint 12 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R88 | `save_notice_preview` IPC가 임의 절대 경로를 파일 쓰기 대상으로 수용 — Tauri Dialog 신뢰 경계 내에서만 호출되나 IPC 직접 우회 시 시스템 파일 접근 가능 | 낮음 (로컬 단독 앱, 인터넷 미노출) | sprint-review 코드 리뷰 F1 | Sprint 13 이후: `canonicalize` 후 `data_root()` 하위 경계 검증, 또는 `output_root()` 제한 경로만 허용 |
| R89 | "저장 후 이동" 미저장 확인 모달에서 저장 실패 시에도 페이지 이동 강행 — `doSaveTemplate`가 예외를 내부 catch하여 re-throw 안 함. DB 오류 등으로 저장 실패해도 navigate 실행됨 | 낮음 (단일 사용자, DB 오류 발생 빈도 낮음) | sprint-review 코드 리뷰 F2 | Sprint 13 이후: `doSaveTemplate` re-throw 패턴으로 수정, 또는 호출부 try-catch 후 조건부 navigate |

---

## Sprint 13 신규 리스크 (sprint-planner)

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R90 | macOS 키체인 접근 OS 프롬프트 — dev 빌드 재컴파일마다 "항상 허용" 재프롬프트. 사용자 혼란 유발 가능 | 중간 | sprint-planner | T6-b 로딩 상태+안내 표시. 프로덕션 서명 빌드에서는 1회 허용 후 재발 없음 |
| R91 | `auto_unlock_with_keychain` + `app_startup_sequence` 공통 로직 추출 시 기존 startup 경로 회귀 위험 | 높음 | sprint-planner | T4-b 리팩토링 후 cipher off/on 양쪽 테스트 전수 실행. 기존 테스트 + 신규 테스트로 양쪽 경로 검증 |
| R92 | config.json `skip_pin_on_launch`가 클라우드 동기화 폴더에 있으면 양 PC 간 설정 충돌 가능 | 중간 | sprint-planner | 설계상 `app_config_dir`(PC별 로컬) 저장 확정. 코드 리뷰에서 경로 재검증 |

---

## Sprint 13 sprint-review 추가 리스크

> 등록일: 2026-06-02

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R93 | `auto_unlock_with_keychain` cipher off 빌드에서 ADR-008 구현 메모("stub/즉시성공")와 실제 코드(keyring 접근 시도) 불일치 — 현재는 프론트 개발 모드 예외로 차단되어 안전하나, 향후 코드 변경 시 혼란 가능 | 낮음 (현재 안전) | sprint-review 코드 리뷰 F1 | 다음 스프린트: `startup.rs AuthStep::Keychain` 분기에 cipher off 동작 명시 주석 추가. ADR-008 구현 메모 실제 동작과 일치하도록 수정 |
| R94 | CLAUDE.md 및 sprint13.md 내 마이그레이션 현황 표기가 실제(V302)와 불일치 — sprint-close가 "V111 최신"으로 기록. 문서 동기화 부재 | 낮음 | sprint-review 발견 | CLAUDE.md는 본 sprint-review에서 V302로 수정 완료. ROADMAP.md의 마이그레이션 현황 섹션 및 sprint13.md 자기검증 문구도 다음 스프린트 초기에 정확한 번호로 일괄 정정 필요 |
