# Risk Register — 2026-06-02 (Sprint 12 코드 리뷰)

> 출처: sprint-review 코드 리뷰 (F1)
> 등록일: 2026-06-02

---

## Sprint 12 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R88 | `save_notice_preview` IPC가 임의 절대 경로를 파일 쓰기 대상으로 수용 — Tauri Dialog 신뢰 경계 내에서만 호출되나 IPC 직접 우회 시 시스템 파일 접근 가능 | 낮음 (로컬 단독 앱, 인터넷 미노출) | sprint-review 코드 리뷰 F1 | Sprint 13 이후: `canonicalize` 후 `data_root()` 하위 경계 검증, 또는 `output_root()` 제한 경로만 허용 |
| R89 | "저장 후 이동" 미저장 확인 모달에서 저장 실패 시에도 페이지 이동 강행 — `doSaveTemplate`가 예외를 내부 catch하여 re-throw 안 함. DB 오류 등으로 저장 실패해도 navigate 실행됨 | 낮음 (단일 사용자, DB 오류 발생 빈도 낮음) | sprint-review 코드 리뷰 F2 | Sprint 13 이후: `doSaveTemplate` re-throw 패턴으로 수정, 또는 호출부 try-catch 후 조건부 navigate |
