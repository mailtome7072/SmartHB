# Risk Register — 2026-06-30 (Sprint 17 계획)

> 출처: sprint-planner 계획 수립
> 등록일: 2026-06-30

---

## Sprint 17 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R117 | Heartbeat 제거 후 stale lock 판정 기준 변경 — mtime 기반 5분 판정이 heartbeat 없이 동작하는지 확인 필요. 비정상 종료 시 mtime이 앱 시작 시점으로 고정되므로 판정 기준 재검증 | 중간 | sprint-planner | lock 생성 시 mtime 기록, 종료 시 삭제. 비정상 종료 시 mtime이 앱 시작 시점 고정이므로 5분 경과 판정은 유효. 단위 테스트로 정상/비정상 종료 시나리오 커버 |
| R119 | Atomic write 시 tmp 파일 잔류 — 앱 비정상 종료 시 backup 디렉토리에 `.tmp` 파일이 남을 수 있음. 누적 시 디스크 공간 점유 | 중간 | sprint-planner | 앱 시작 시 backup 디렉토리의 `.tmp` 파일 정리 로직 추가 (T2 구현 시 함께 처리). 정리 실패 시 경고만 출력(앱 시작 차단하지 않음) |

---

## Sprint 17 코드 리뷰 추가 등록 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R120 | Heartbeat 제거 후 실제 stale lock 취약점 확인 — STALE_THRESHOLD_SECONDS=300, last_heartbeat는 acquire 시 1회만 설정. 5분 이상 사용 시 두 번째 PC가 자동 점유 가능. 집<->교습소 동시 사용 없음 전제이므로 즉각 위험은 낮으나 전제 변경 시 데이터 손상 위험 | 높음 | sprint-review 코드 리뷰 (F1) | Sprint 18에서 STALE_THRESHOLD_SECONDS를 86400(24h)으로 상향하거나 config.json 기반 "마지막 사용 PC" 판정으로 전환. 현재는 사용자 운영 패턴(동시 사용 없음)에 의존 |
| R121 | rollback 파일명 초 단위 정밀도 — auto_restore_with_retry retry 루프에서 1초 이내 두 번 실행 시 동일 rollback 경로로 rename이 발생해 원본 DB rollback 파일 덮어쓰기 | 높음 | sprint-review 코드 리뷰 (F2) | Sprint 18에서 generate_rollback_filename에 loop 인덱스 또는 나노초 컴포넌트 추가 |
| R122 | WAL 체크포인트 실패 시 pool.close() 미실행 — DB 폴더 변경 실패 후 구 경로 pool이 열린 상태 유지 | 중간 | sprint-review 코드 리뷰 (F3) | Sprint 18에서 early return 전 pool.close() 호출 추가 또는 WAL checkpoint retry 구현 |
| R123 | cleanup_stale_tmp_backups spawn_blocking 누락 — async startup 경로에서 blocking std::fs 직접 호출 | 중간 | sprint-review 코드 리뷰 (F4) | Sprint 18에서 tokio::task::spawn_blocking 래핑. 네트워크 드라이브 환경에서 startup 지연 가능성 |
