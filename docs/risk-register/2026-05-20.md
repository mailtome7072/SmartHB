# Risk Register — 2026-05-20

> Sprint 2 (기반 도메인 백엔드 + Sprint 1 잔여) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| ~~R9~~ | ~~PI-05 미결정~~ — **해소 (2026-05-20)**: 자동 채번 확정. `MAX(serial_no)+1` + `BEGIN IMMEDIATE`. V101 마이그레이션에 직접 반영, 추가 마이그레이션 불필요 | ~~중간~~ | sprint-planner | 해소됨 |
| ~~R10~~ | ~~V100 ALTER TABLE 호환성~~ — **무효화 (2026-05-20)**: V100 마이그레이션 자체 폐기 (salt 를 DB 에 저장 불가, chicken-and-egg). R10 도 함께 종결 | ~~낮음~~ | sprint-planner | T2 이연으로 무효화 |
| R11 | 부분 인덱스 SQLite 호환 — `CREATE UNIQUE INDEX ... WHERE effective_to IS NULL` 은 SQLite 3.8.0+ 필요 | 낮음 | sprint-planner | Tauri 번들 SQLite 버전 3.39+ (2022). 호환 문제 없음 예상. 미지원 시 어플리케이션 레벨 UNIQUE 검증으로 대체 |
| R12 | **(신규)** R6 salt 이전이 Sprint 2 범위에서 불가 — DB 안에 salt 보관은 chicken-and-egg | 중간 | Sprint 2 T1 진행 중 발견 (2026-05-20) | Sprint 3 마법사 통합 시 `{data_root}/salt.bin` 평문 파일로 이전. PBKDF2 의 salt 는 비밀 아니므로 평문 보관 정당 (OWASP) |

---

> sprint-review 코드 리뷰에서 추가 등록된 이슈 (2026-05-20)

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R13 | `audit::try_record` details 파라미터에 원생 이름(PII) 마스킹 없이 기록 — `create_student`/`update_student`/`withdraw_student` 3곳 | 중간 | sprint-review 코드 리뷰 (2026-05-20) | Sprint 3 감사 로그 UI 전에 이름 마스킹 또는 `None` 전달로 수정 (액션 아이템 A1) |
| R14 | `list_students` / `list_codes` 페이지네이션 누락 — 현재 소규모 교습소(50명 이내) 실질 영향 낮음 | 중간 | sprint-review 코드 리뷰 (2026-05-20) | Sprint 3 목록 화면 구현 시 `page/limit` 파라미터 추가 (액션 아이템 A3) |
| ~~R15~~ | ~~`startup::exit_hook`에서 `release_lock_atomic()` 대신 `remove_file` 직접 호출~~ — **해소 (2026-05-20)**: v0.2.0 배포 전 sprint2 브랜치에서 직접 수정. `release_lock_atomic()` 호출 + Ok/Err 패턴 매치로 best-effort 처리. `lock::release_lock_atomic` 을 `pub(crate)` 으로 노출 | ~~중간~~ | sprint-review 코드 리뷰 (2026-05-20) | 해소됨 |
