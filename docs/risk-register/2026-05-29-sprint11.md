# Risk Register — 2026-05-29 (Sprint 11 리뷰)

> 출처: sprint-review 코드 리뷰 + 인계 사항 검토
> 등록일: 2026-05-29
> 이전 리스크 등록: `docs/risk-register/2026-05-28-sprint11.md` (R73~R76)

---

## Sprint 11 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R77 | 청구 50명 3초 이내 실측 미진행 — `generate_bills` 인메모리 35 테스트 0.21s 통과했으나 실 DB(50명 시드) + UI 왕복 성능 미측정 (PRD §5.6) | 중간 | sprint-review 인계 | develop 머지 후 `pnpm tauri:dev` + 50명 시드 수동 시간 측정. 3초 초과 시 IPC 분리(청구 생성 → 스트리밍 응답) 또는 SQL 인덱스 보강 검토 |
| R78 | SQLite SQLITE_LIMIT_VARIABLE_NUMBER(기본 999) — `calendar.rs` F4 IN batch 쿼리에서 `deadline_months` 수만큼 바인딩. 원생 수 × 마감 월 수가 999 초과 시 SQLite 오류 가능 | 낮음 | sprint-review 코드 리뷰 | 실질 학생 수 50명, 마감 월 수 최대 24개 → 최대 24 바인딩으로 안전. 확장 시 LIMIT 999 이하 청크 분할 처리 추가 |
| R79 | 카드 계열 결제수단 UI 휴리스틱 (`code === 'card'`) vs 백엔드 `is_card_type` 불일치 가능 — 원장이 관리자 화면에서 새 카드 계열 결제수단(`is_card_type=1`)을 추가해도 PaymentsView UI는 카드사 선택 UI를 표시하지 않음 | 낮음 | sprint-review 인계 (PI 디자인 결정) | 현재 시드 결제수단 단순(현금/카드 2종)이라 실질 위험 미미. 향후 결제수단 추가 기능 도입 시 UI를 `is_card_type` 필드 기반으로 전환. 백엔드 IPC가 카드사 누락 시 에러 반환하므로 데이터 정합성은 보장됨 |
| R80 | CloseMonthDialog + summaryQuery 실패 시 마감 버튼 무반응 (F1) — summaryQuery 독립 실패 시 '당월 청구 마감' 버튼이 표시되나 다이얼로그 미렌더링 | 중간 | sprint-review 코드 리뷰 | Sprint 12 또는 Phase 4 마감 시 `showCloseButton` 조건에 `summary !== undefined` 추가. 단기: 두 쿼리 병렬 실행으로 실패 동시 발생 가능성 낮음 |
| R81 | PaymentsView payerName 단독 입력 소실 (F3) — isPaid/paymentMethodId 미설정 상태에서 payerName만 입력한 행은 저장 대상에서 제외됨. 입력 소실에 대한 UI 안내 없음 | 낮음 | sprint-review 코드 리뷰 | Sprint 12에서 dirtyEntries 필터에 `d.payerName !== ''` 추가 또는 안내 메시지 표시. 현재 운용 패턴상 payerName 단독 입력 케이스 희소 |
| R82 | 마감 후 추가 청구 정책 모호 — `generate_bills` 가 INSERT OR IGNORE 라 마감(closed) 된 월에 신규 학생이 등록되면 그 학생의 청구가 `draft` 로 신규 추가됨. 사용자가 확정·재마감 시 같은 월에 두 시점의 `closed_at` 이 공존. 회계상 "마감"의 본질(시점 잠금)과 충돌 가능 | 중간 | post-Sprint 11 사용자 발견 (마감 정책 검토) | Sprint 12 정책 결정 필요 — (a) 마감 후 신규 청구를 차단 / (b) 신규 청구를 별도 보류 상태(`pending_after_close`) / (c) 현재 동작 유지 + UI 안내. PRD §4.9.7 보강 필요 |
