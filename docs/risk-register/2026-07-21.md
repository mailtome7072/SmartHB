# Risk Register -- 2026-07-21 (Sprint 22 계획)

> 출처: sprint-planner
> 등록일: 2026-07-21

---

## Sprint 22 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R139 | 부분 소진 모델 전환 시 기존 보강 매칭 쿼리 8개 파일(calendar/attendance/expiration/diagnosis/students) 일괄 변경 필요 -- 누락 시 잔여분 계산 불일치로 보강 대상 목록/요약/소멸/진단 결과 오류 | 높음 | sprint-planner | T4에 회귀 위험 체크리스트 8항목 명시. 각 항목별 단위 테스트 추가. 전수 변경 후 `grep -rn "makeup_attendance_id IS NULL"` 잔여 확인 |
| R140 | 백필 마이그레이션이 실 DB의 예상치 못한 데이터 패턴(부분 매칭 없는 정상 데이터, 이미 소멸된 결석, 고아 보강 등)과 충돌 가능 | 중간 | sprint-planner | 멱등(idempotent) 설계 필수. makeup_expired 제외 정책 확정. 실행 전 자동 백업. 보정 건수 로그로 사후 확인 가능. 인메모리 단위 테스트에서 다양한 데이터 패턴 커버 |
| R141 | 부분 소진 상태 표현이 기존 `makeup_done`/`absent` 2값 모델과 호환되지 않을 수 있음 -- ADR 결정에 따라 새로운 상태값 또는 링크 테이블 도입 시 기존 코드 전수 점검 필요 | 높음 | sprint-planner | T0 ADR에서 기존 status 값과의 호환성을 최우선 평가 기준으로 명시. 가능하면 기존 2값 모델을 유지하면서 누적 컬럼만 추가하는 방향 검토 |

## Sprint 22 코드 리뷰 후 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R01 | `cancel_makeup_impl` 루프 내 N+1 쿼리 — 취소된 보강에 배분된 결석 수(N)만큼 잔여 재계산 쿼리를 개별 실행 (최대 2N 쿼리). 현재 50명 규모에서 실용적 영향 미미하나 원생 100명+ 확장 시 지연 가능 | 중간 | sprint-review Sprint 22 코드 리뷰 F1 | 다음 makeup.rs 수정 스프린트에서 IN 절 배치 쿼리로 전환 (A127). 현재 규모에서 수용 |
