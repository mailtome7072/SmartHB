# Risk Register — 2026-05-21

> Sprint 3 (원생 관리 프론트 + 초기 설정 마법사 + 글로벌 검색 + 접근성 기반) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R16 | `tauri-plugin-dialog` 양 OS 호환성 미확인 — 마법사 폴더 선택 기능의 핵심 의존성 | 중간 | sprint-planner | T7에서 양 OS 빌드 검증. 실패 시 `rfd` crate 직접 사용 검토 |
| R17 | Pretendard woff2 파일 크기로 Tauri 번들 증가 | 낮음 | sprint-planner | subset (한글+영문+숫자+기호)만 포함, 전체 포함 시 ~2MB 이내로 영향 미미 |
| R18 | salt.bin 이전 시 기존 v0.1.0/v0.2.0 설치자 마이그레이션 경로 — 마법사 재진입 시 Keychain->파일 자동 이전 필요 | 중간 | sprint-planner | 현재 사용자 데이터 없는 시점이므로 이전 호환성은 v1.0 릴리즈 전 재검토. 마법사 코드에 Keychain 잔존 salt 감지 로직 포함 |
| R19 | 글로벌 검색 한글 자모 분해 정확도 — 초성 검색, 자모 부분 일치 구현 복잡도 | 낮음 | sprint-planner | `hangul-js` 또는 자체 초성 분해 유틸 구현. 자모 분해 알고리즘은 표준(유니코드 한글 자모 분해)이므로 정확도 이슈 낮음 |
| R20 | `paths::data_root()` 하드코딩 — 프로덕션 DB가 클라우드 폴더가 아닌 개발 경로를 참조 | 높음 | sprint-review 코드 리뷰 | ✅ 해소 (`82eb1b2`, 2026-05-21) — `paths::data_root()` 가 `config.json` 의 `cloud_folder_path` 를 동적으로 참조하도록 변경. salt.bin 이전(R18) 은 Sprint 4 에서 별도 처리 |
| R21 | `dialog:allow-open` 권한이 디렉토리 전용으로 좁혀지지 않음 — 파일 선택 다이얼로그도 허용 가능 상태 | 중간 | sprint-review 코드 리뷰 | `tauri-plugin-dialog` 2.x 하위 권한 분리 여부 확인 후 `dialog:allow-open-directory`로 교체. Sprint 4 진입 전 hotfix로 처리 가능 |
| R22 | `window.confirm()` 퇴교 확인 다이얼로그 — Tauri WebView에서 동작하나 50대 사용자 친화적이지 않고 스타일링 불가 | 중간 | sprint-review 코드 리뷰 | ✅ Sprint 4 T1에서 shadcn/ui AlertDialog로 교체 예정 |

---

> Sprint 4 (Phase 1 품질 안정화) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R23 | shadcn/ui 초기 설정 미완료 — `components.json` 부재 시 AlertDialog 추가 실패 | 중간 | sprint-planner | T1 착수 시 shadcn init 여부 확인. 미설정 시 `npx shadcn@latest init` 선행 |
| R24 | dnd-kit과 React 19 호환성 미확인 — 공식 지원 여부 검증 필요 | 중간 | sprint-planner | T10 착수 전 호환성 확인. 비호환 시 네이티브 HTML Drag API로 대체 |
| R25 | 상태바 IPC 응답 미연결 — lock/backup/sync 모듈 IPC 구조 파악 필요 | 중간 | sprint-planner | ✅ 해소 (T3, `d725747`, 2026-05-21) — checkLockStatus/listBackups/checkSyncStatus IPC 연결 완료 |

---

> Sprint 4 코드 리뷰 (sprint-review, 2026-05-22) 식별 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R26 | DnD 필터링 중 숨겨진 항목의 sort_order 충돌 — 활성 필터 적용 상태에서 DnD 정렬 시 보이는 행만 재번호 부여, 숨겨진 행과 sort_order 겹침 가능 | 중간 | sprint-review 코드 리뷰 | 단일 사용자·소량 데이터로 사용자 체감 오류 없음. Sprint 5에서 필터 무관 전체 재정렬 또는 DnD 시 필터 '전체' 강제 전환으로 개선 |
