# Risk Register — 2026-05-22

> Sprint 5 (Phase 1.5b 안정화) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R27 | `tauri-plugin-single-instance`가 Tauri 2.x와 호환되지 않을 가능성 -- 플러그인 API 변경 또는 macOS/Windows 양 OS 동작 차이 | 중간 | sprint-planner | Tauri 2.x 공식 플러그인 목록에서 호환성 확인. 비호환 시 `fs2` crate로 프로세스 레벨 락 파일 구현 (대안) |
| R28 | V201 시드 교체 마이그레이션이 기존 DB에서 조건 감지 실패 -- 시드 행이 예상과 다른 상태일 때 교체가 누락되거나 중복 삽입 발생 | 낮음 | sprint-planner | pre-release 시점이므로 실제 사용자 데이터 없음. 조건 감지 로직은 방어적 작성하되 실패해도 기존 데이터 보존 (INSERT OR IGNORE). 인메모리 DB 테스트로 edge case 검증 |
| R29 | cross-env가 Tauri dev 환경에서 NODE_OPTIONS 전달 실패 -- `pnpm tauri:dev`가 Next.js dev를 내부 호출하는 구조에서 환경 변수 전파 경로 불확실 | 낮음 | sprint-planner | `pnpm dev`(Next.js only)와 `pnpm tauri:dev`(Tauri + Next.js) 양쪽에서 환경 변수 전달 검증. tauri:dev가 별도 프로세스로 Next.js를 spawn하는 경우 `tauri.conf.json`의 `beforeDevCommand`에도 cross-env 적용 필요 |

---

> Sprint 6 (Phase 2 학사 스케줄) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R30 | 3개월 캘린더 커스텀 구현 복잡도 -- shadcn/ui Calendar는 단일 월 위젯이므로 3개월 합성 + 교습기간 하이라이트 + 배지 오버레이가 예상보다 복잡할 수 있음 | 중간 | sprint-planner | 최소 기능부터 점진 구현: 1) 기본 월 그리드 3개 배치, 2) 교습기간 하이라이트, 3) 배지 오버레이 순서. 복잡도 초과 시 드래그 이동을 Sprint 7로 이연 |
| R31 | 공휴일 API 의존성 -- 공공데이터포털(인증키 만료/API 변경) 또는 date.nager.at(서비스 중단/한국 데이터 부정확) 위험. 대체공휴일 누락 가능 | 낮음 | sprint-planner | 빌드 타임 1회 수집이므로 런타임 영향 없음. 두 API 모두 실패 시 수동 SQL 작성 대안 보유. 수집 후 단위 테스트로 주요 공휴일 존재 검증. 2030년 만료 전 재실행으로 갱신 |
| R32 | OnceLock 리팩토링(T3)이 프로덕션 paths 동작에 영향 -- 전역 상태 관리 방식 변경 시 앱 시작 시퀀스에 부작용 발생 가능 | 중간 | sprint-planner | 리팩토링 후 `app_startup_sequence` 통합 테스트 + `pnpm tauri:dev` 수동 검증 필수. 영향 범위가 크면 `#[cfg(test)]` 분리 방식으로 프로덕션 코드 변경 최소화 |

---

> Sprint 6 코드 리뷰 (2026-05-22 sprint-review) 에서 식별된 Medium 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R33 | CalendarCell.tsx `codeBadgeClass` + ThreeMonthCalendar.tsx `draggableEventIds` 에 시스템 코드명 6종을 한국어 문자열로 하드코딩 — 코드명 변경 시 두 위치 동시 수정 필요, 누락 시 배지 색상 오류 및 드래그 차단 로직 불일치 | 중간 | sprint-review 코드 리뷰 | `ScheduleEventListItem`에 `is_system_reserved` 필드를 JOIN으로 추가 → 프론트엔드가 코드명 대신 플래그 기반으로 판단하도록 변경 (A23, Sprint 7 검토) |
| R34 | 교습기간 범위 밖 드롭 시 백엔드·프론트엔드 양쪽 모두 가드 없음 — 원장이 의도치 않게 교습기간 외 날짜에 일정을 배치해도 오류 없이 저장됨 | 낮음 | sprint-review 코드 리뷰 | 현재는 의도된 유연성(원장 재량)으로 결정. 배포 후 사용자 피드백에 따라 UI 경고(토스트/드롭 거부 피드백) 추가 여부 결정 (A25) |

---

> Sprint 7 (Phase 2 carry-over 해소) 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R35 | Keychain 캐싱(T1) 도입 후 메모리 내 DB 암호화 key 노출 시간 증가 — 앱 실행 전체 시간 동안 key가 메모리에 존재 | 중간 | sprint-planner | ZeroizeOnDrop으로 프로세스 종료 시 자동 폐기. Mutex<Option<>>으로 명시적 invalidation 가능. PRD §5.5 단일 사용자 로컬 앱 보안 모델 대비 수용 가능한 trade-off |
| R36 | salt.bin 이전(T2) 시 마이그레이션 중 crash로 Keychain salt + 파일 salt 동시 존재/불일치 가능 | 낮음 | sprint-planner | 원자적 마이그레이션: Keychain 읽기 → 파일 쓰기 → Keychain 삭제. 각 단계 실패 시 다음 시작에 자동 재시도. 양쪽 존재 시 파일 우선 |
| R37 | device_id 영속화(T3) 후 클라우드 동기화로 양 PC에 동일 device.id 복제 — 동일 ID로 양 PC가 인식되면 lock 구분 불가 | 중간 | sprint-planner | device.id를 OS 로컬 전용 경로(Tauri app_config_dir)에 저장하여 클라우드 동기화 대상에서 제외. 또는 smarthb/ 하위에 두되 `.nosync` 마킹 검토 |
| R38 | 교습기간 배치 제약 강화(T7) 후 기존 배치 데이터와 불일치 — Sprint 6에서 교습기간 외에 배치한 일정이 수정 불가 | 낮음 | sprint-planner | pre-release 상태(실사용자 데이터 없음)이므로 기존 데이터 불일치 가능성 극히 낮음. 개발 DB 리셋으로 해소 |

---

> Sprint 7 코드 리뷰 (2026-05-22 sprint-review) 에서 식별된 Medium 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R39 | `StudyPeriodEditor` T6: `createStudyPeriod` 성공 + `confirmStudyPeriod` 실패 시 미확정 교습기간이 DB에 잔존 → `create_study_period` overlap 검사(is_confirmed 필터 없음)에 의해 재시도 불가, orphan 수동 삭제 필요 | 중간 | sprint-review 코드 리뷰 (T6, StudyPeriodEditor.tsx:104) | `create+confirm`을 단일 원자 IPC로 통합하거나 overlap 검사에 `AND is_confirmed = 1` 추가. 다음 스프린트 hotfix 후보 |

---

> Sprint 7 carry-over (Session #2 high-effort code review 잔여 9건 + Session #4 1건)

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R40 | `is_salt_corrupted`: 1~31바이트 NULL이 혼합된 partial-NULL 패턴은 손상으로 감지되지 않음 — `bytes.iter().all(|&b| b == 0)` 조건이 모든 바이트가 0인 경우만 손상 판정 | 높음 | I-S2-2, Session #2 code review | `is_salt_corrupted` 검사 강화: ALL-zero 대신 first 8바이트 체크 또는 복수 조건 적용. 후속 hotfix 검토 |
| R41 | `set_password` 재진입 가드 없음 — 동시 호출 시 두 번째 호출이 진행 중인 keyring 저장을 덮어써 race 발생 가능 → PC-B 영구 lockout 위험 | 높음 | I-S2-3, Session #2 code review | `set_password` 시작 시 flag check 또는 Mutex guard 추가. 단일 사용자 데스크톱 환경에서 concurrent 호출 가능성 낮으나 후속 스프린트에서 보강 |
| R42 | `CRED_CACHE` static OnceLock — process exit 시 Drop 미보장. ZeroizeOnDrop 주석이 "종료 시 자동 폐기" 로 읽히나 static lifetime 객체는 Drop 미실행 환경 존재 | 높음 | I-S2-4, Session #2 code review | 모듈 주석 정정 필수. shutdown hook에서 `invalidate_credential_cache()` 명시적 호출 추가 검토 |
| R43 | `check_auth_status`가 legacy keyring 마이그레이션을 트리거하지 않음 — 기존 설치에서 salt.bin 미존재 + legacy keyring 잔존 시 startup 마다 `NotInitialized`로 잘못 분류 가능 (I-S2-5) | 높음 | I-S2-5, Session #2 code review | `check_auth_status`에서 `salt_exists()` 호출 경로가 이미 legacy keyring fallback 포함(`salt_exists_at` → `keyring_get_or_none`) — 재검증 필요. 단, startup sequence와의 순서 의존 확인 |
| R44 | 테스트 `load_salt_backs_up_corrupted_file`이 dev 환경의 실제 Keychain 항목을 삭제할 수 있음 — `paths::salt_path()`가 dev DB 경로 기반이라 테스트 실행 중 실제 salt 복구 경로에 진입 가능 | 높음 | I-S2-6, Session #2 code review | 해당 테스트에 `#[ignore]` 마킹 또는 mock salt_path 주입. 후속 스프린트에서 반드시 처리 |
| R45 | `tokio::join!` 시점 concurrent `verify_password` / `get_cached_or_load_key` race — 두 Keychain 다이얼로그가 동시 표시될 수 있으며 AC-T1-1(다이얼로그 1회) 잠재 위반 | 중간~높음 | I-S2-7, Session #2 code review | startup sequence에서 `verify_password` 단독 호출 후 나머지 cipher key 조회 순서 보장. tokio::join! 사용 지점 점검 |
| R46 | `Mutex::lock().expect("cred_cache poisoned")` — 단일 panic으로 Mutex가 poisoned 상태가 되면 이후 모든 캐시 접근이 panic → 앱 영구 brick | 중간 | I-S2-8, Session #2 code review | `lock().ok()` 또는 `unwrap_or_else` 패턴으로 poison 복구. 또는 `std::panic::catch_unwind` 래핑 검토 |
| R47 | `migrate_keyring_salt_to` audit 누락 — salt 마이그레이션 이벤트가 audit 로그에 기록되지 않음 | 중간 | I-S2-9, Session #2 code review | `audit::try_record(AuditEventType::SecurityEvent, Some("salt-migration"), None)` 추가 |
| R48 | 잡다 Low 이슈 묶음 (I-S2-10): timestamp 충돌(동초 백업), `with_extension` 함정(경로에 `.` 포함 시), salt buffer ZeroizeOnDrop 미적용, `device.id` 0o644 권한(0o600 권장), data_root fallback 위험, stale doc comment, sprint-planner MEMORY 잘못된 키 리터럴 | 낮음 | I-S2-10, Session #2 code review | 각 항목 후속 스프린트 spare time에 처리. sprint-planner MEMORY 키 리터럴은 즉시 수정 검토 |
| R49 | `CalendarCell.tsx:154-156` `hasHoliday`/`hasAssessment`/`nonAssessmentEvents`에 시스템 코드 식별 한국어 리터럴 잔존 — T4에서 `is_system_reserved` 플래그로 시스템 여부는 처리했으나 공휴일/단원평가 특수 UI 분기는 별도 비즈니스 의도 | 낮음 | I-S4-1, Session #4 code review | 후속 스프린트에서 `code_intent` 컬럼 또는 별도 플래그 도입 검토. 현재는 코드명 리터럴이 UNIQUE 제약으로 충돌 불가하여 기능 영향 없음 |
