# Risk Register — 2026-05-30 (post-Sprint 11 develop 보완 리뷰)

> 출처: sprint-review 코드 리뷰 (develop 보완 2건 — `945e4a7`, `c93399e`)
> 등록일: 2026-05-30
> 이전 리스크 등록: `docs/risk-register/2026-05-29-sprint11.md` (R77~R82)

---

## post-Sprint 11 develop 보완 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R83 | `update_bill_impl` — 수납완료 여부 확인 쿼리(`SELECT is_paid FROM payments WHERE bill_id = ?`)가 트랜잭션 외부 단건 조회 (F1) — `status` 조회와 `is_paid` 조회 사이에 다른 프로세스가 수납 취소하면 read-inconsistency 발생 가능. 단일 PC 오프라인 앱이라 실질 위험 낮지만 설계 일관성 결여 | 낮음 | sprint-review 코드 리뷰 (`billing.rs:294`) | Sprint 12 또는 이후 — `status`와 `is_paid`를 단일 JOIN 쿼리로 통합 (`SELECT b.status, p.is_paid FROM bills b LEFT JOIN payments p ON p.bill_id=b.id WHERE b.id=?`). 하드 블로커 아님 |
| R84 | PIN 전환 — `verify_password`에 `validate_pin` 미적용 (F2) — `set_password`/`reset_password_with_code`에는 형식 게이트가 있으나 `unlock_db`→`verify_password` 경로는 빈 문자열 체크만. 이미 저장된 PIN과 비교하므로 형식 불일치 시도는 자동 인증 실패로 귀결되어 실질 보안 영향 없음 | 낮음 | sprint-review 코드 리뷰 (`auth.rs:664`) | 설계상 의도적 생략 (ADR-007: "verify_password는 형식 검증 없이 키 비교만 수행"). 기록 목적으로 등록. 정책 변경 시 ADR-007 업데이트 후 적용 |
| R85 | PIN 10^6 키스페이스 — 클라우드 계정 유출 시 오프라인 브루트포스 가능 (ADR-007 명시 수용). 복구 코드 12자리 고엔트로피 유지 + PBKDF2 600K iter 비용이 완화책 | 낮음 (명시 수용) | ADR-007 — sprint-review 확인 | ADR-007에 위협 모델·완화책·수용 근거 기록 완료. 향후 생체인증(Touch ID/Windows Hello) 도입 시 재검토 (PRD §5.5) |

---

## post-Sprint 11 develop 보완 4커밋 추가 리스크 (2026-05-30)

> 출처: sprint-review 코드 리뷰 (`70c59a1` `c1ae063` `2a964b0` `29fbe93`)

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R86 | `update_bill_impl` — `current_status` 조회 결과가 존재 확인 목적으로만 쓰이고 status 값 자체는 미사용(dead variable 패턴). status 조회와 is_paid 조회 2회 왕복으로 불필요한 DB 쿼리 발생. clippy는 `ok_or_else()?` 패턴으로 통과(경고 없음). | 낮음 | sprint-review 코드 리뷰 (`billing.rs:287~293`) | R83 후속 — status + is_paid 조회를 단일 LEFT JOIN으로 통합 시 자연 해소. Sprint 12 carry-over |
| R87 | `BillingSummaryView` — 년/월 토글 UI가 `type="checkbox"` 사용 (상호 배타 동작이나 `radio`가 의미론 일치). 접근성 측면에서 스크린리더가 체크박스로 안내하여 혼란 가능. 50대 사용자 대상 앱에서 접근성 우선순위 고려 필요. | 낮음 | sprint-review 코드 리뷰 (`BillingSummaryView.tsx:90`) | Sprint 12 이후 — `type="radio"` + `role="radiogroup"` 전환. 시각 렌더링은 동일, 의미론 정확성 개선. 하드 블로커 아님 |
