# Risk Register — 2026-05-30 (Sprint 12 계획)

> 출처: sprint-planner 계획 수립 시 식별
> 등록일: 2026-05-30
> 이전 리스크 등록: `docs/risk-register/2026-05-30.md` (R83~R87)

---

## Sprint 12 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R88 | `html-to-image` foreignObject 방식이 배경서식 이미지 해상도를 왜곡할 가능성 — 특히 고해상도 배경서식(2000px+)에서 Canvas 변환 시 품질 저하 우려 | 중간 | sprint-planner | T7 초반에 단일 이미지 PoC로 품질 확인. `toPng()` 의 `pixelRatio` 옵션(2x)으로 보정 가능. 최악의 경우 `html2canvas` 대안 전환 (API 유사) |
| R89 | 50장 순차 생성 시 30초 초과 가능성 — `toPng()` 호출당 DOM → SVG → Canvas → PNG 파이프라인 비용 누적 | 중간 | sprint-planner | pixelRatio 1x 제한 + 이미지 크기 최적화(카카오톡 전송 기준 1280px 폭). 미달 시 Web Worker 병렬 처리 또는 `requestAnimationFrame` 인터리브로 UI 스레드 양보 |
