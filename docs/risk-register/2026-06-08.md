# Risk Register — 2026-06-08 (Sprint 16 계획)

> 출처: sprint-planner 계획 수립
> 등록일: 2026-06-08

---

## Sprint 16 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R106 | CSV 가져오기 EUC-KR 인코딩 — 한국어 엑셀 기본 CSV가 EUC-KR이며, UTF-8 BOM 없는 파일과 혼재 가능. 잘못된 인코딩 감지 시 원생 이름 깨짐으로 UAT 데이터 손상 | 높음 | sprint-planner | `encoding_rs` crate으로 BOM/heuristic 자동 감지 + 미리보기 단계에서 사용자 확인 후 임포트 실행. 임포트 전 미리보기 필수 |
| R107 | 초기 실사용 피드백 볼륨 초과 — 50대 원장 실사용에서 예상보다 많은 UX 개선 요청 발생 시 T6 버퍼(4h) 초과 | 중간 | sprint-planner | 피드백 Critical/High만 Sprint 16 내 반영. Medium/Low는 Post-MVP backlog. SHOULD 작업(T7~T8)을 이연하여 T6 버퍼 확대 가능 |
| R108 | 양 PC 동기화 클라우드 지연 — MYBOX/iCloud Drive 동기화 지연으로 app.lock 충돌 또는 DB 정합성 문제 | 중간 | sprint-planner (R2 재확인) | T3에서 동기화 완료 대기 시간 측정. 5분 임계값 설정으로 강제 점유 정상 동작 확인. 심각 시 동기화 대기 UI 메시지 추가 |
| R109 | macOS .dmg 서명 미비 — Apple 공증(notarization) 없이 배포 시 "확인되지 않은 개발자" 경고로 설치 차단 가능 | 중간 | sprint-planner | 개인 사용이므로 Gatekeeper 우회(Control+클릭 열기) 안내. 향후 Apple Developer 계정 등록 검토(Post-MVP) |
| R110 | cipher ON 프로덕션 빌드 + 데이터 이관 — cipher ON 빌드에서 CSV 가져오기 시 암호화 DB에 정상 쓰기 검증 필요 | 높음 | sprint-planner | T3 빌드 검증에서 cipher ON 인스톨러로 CSV 임포트 테스트 포함. cipher off dev 빌드와 동작 차이 없는지 확인 |
| R111 | DB 폴더 변경 중 강제 종료 — copy-then-switch 도중 전원 차단 시 복사본 불완전 + config.json 불일치 가능 | 높음 | sprint-planner (PI-16 확정) | copy-then-switch 전략으로 원본 보존. config.json 업데이트는 복사 완료 후 마지막 단계. 중간 실패 시 원래 경로 자동 복귀. `ntfs-power-loss-pattern` fsync 적용 |
| R112 | salt.bin 이전 후 양 PC 불일치 — 한 PC에서 폴더 변경 후 다른 PC의 config.json이 구 경로를 가리킴 (config.json은 PC별 로컬 저장) | 중간 | sprint-planner (PI-16 확정) | config.json은 `app_config_dir`(PC별 로컬) 저장. 폴더 변경 시 양 PC 모두 수동 변경 필요 — UI에 안내 메시지 표시. 또는 DB 파일 위치를 DB 내부에 자기 참조로 저장하는 방안 ADR에서 검토 |
