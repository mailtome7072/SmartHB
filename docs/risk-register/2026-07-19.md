# Risk Register -- 2026-07-19 (Sprint 20 계획)

> 출처: sprint-planner
> 등록일: 2026-07-19

---

## Sprint 20 계획 수립 시 식별된 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R131 | CASCADE 삭제 시 PRAGMA foreign_keys=ON 미적용 가능성 — `delete_bill` 실행 후 payments가 고아로 잔존하여 데이터 정합성 훼손 | 높음 | sprint-planner | T3 단위 테스트에서 FK ON 환경 삭제 후 payments 0건 검증. `db.rs:117` 풀 초기화 시 `PRAGMA foreign_keys=ON` 설정 재확인. 인메모리 테스트에서도 동일 PRAGMA 적용 |
| R132 | 실 DB 보정 중 정상 청구 오삭제 — 프로덕션 수납 데이터 소실 | 높음 | sprint-planner | T5 보정 절차에 사전 백업 필수화. 1건씩 UI에서 확인 후 삭제. 감사 로그(BillDeleted)로 삭제 이력 추적 가능. 이상 시 사전 백업본 복원 |
| R133 | 교습기간 미등록 월 차단이 기존 워크플로우 변경 — 원장이 교습기간 없이 청구 생성하던 관행과 충돌 시 혼란 | 중간 | sprint-planner | 차단 에러 메시지에 "교습기간 등록" 화면 안내 명시. 실사용 전 원장에게 변경사항 사전 안내 |
| R137 | ~~delete_bill_impl 트랜잭션 미사용 — SELECT 체크와 DELETE 사이 TOCTOU 이론적 가능성~~ | 낮음 | sprint-review 코드 리뷰 F1 | **✅ 해결(2026-07-19)**: `delete_bill_impl`을 `pool.begin()` + `SELECT 1 FROM bills LIMIT 0`(BEGIN IMMEDIATE 패턴)으로 감싸 SELECT 가드~DELETE 원자화. 감사 로그는 커밋 후 best-effort. 테스트 4건 통과(CASCADE 포함) |
