# Risk Register — 2026-05-26 (Sprint 9)

> 출처: sprint-review 코드 리뷰 + 도메인 설계 검토
> 등록일: 2026-05-26

---

## Sprint 9 신규 리스크

| ID | 설명 | 영향도 | 출처 | 대응 계획 |
|----|------|--------|------|-----------|
| R63 | `mark_makeup_absent` / `batch_create_makeups` IPC — dead code 잔존. UI 호출 제거(J5/J7 폐기) 이후 백엔드 코드가 정리되지 않아 빌드 시 미사용 경고 위험 | 낮음 | sprint-review 코드 리뷰 | Sprint 10 carry-over: `mark_makeup_absent` + `batch_create_makeups` + `audit::MakeupAbsent` variant 제거 또는 `#[allow(dead_code)]` 명시 후 정리 |
| R64 | `get_absence_history`의 페이지네이션 미적용 — 결석 이력이 많은 장기 원생에서 응답이 지연될 수 있음 (정렬: DESC, 전수 조회) | 낮음 | sprint-review 코드 리뷰 | Sprint 10~11에서 `/pagination.rs` 헬퍼 적용 검토. 현재 PRD 요건(50명 이하 단독 교습소)에서는 허용 범위 |
| R65 | `create_makeup_with_absences_impl`의 결석 단건 루프 검증 — `absence_ids`가 많을 때 N회 SELECT 발생. 현재 PRD 규모(30일×1학생)에서는 문제없으나, 20건+ 선택 시 미미한 성능 저하 가능 | 낮음 | sprint-review 코드 리뷰 | Sprint 10 carry-over: IN 절을 `query_as` + `JSON_EACH` 패턴으로 교체 가능. 현재는 안전 (검증 정확성 우선) |

---

## Sprint 10 Carry-over 정리 (sprint9 이연 항목)

| 항목 | 유형 | 우선도 |
|------|------|--------|
| `mark_makeup_absent` dead code 정리 | 기술 부채 | High |
| `batch_create_makeups` dead code 정리 | 기술 부채 | High |
| `makeup_attendances.status='makeup_absent'` CHECK 제약 마이그레이션 정리 | 선택적 정리 | Medium |
| `get_attendance_grid` N+1 → batch 쿼리 (A42 이연) | 성능 | Medium |
| R44 salt buffer ZeroizeOnDrop (A44 이연) | 보안 | Medium |
| 반응형 폰트/셀 너비 (A45 이연) | UX | Medium |
| 한글 자모 부분 일치 검색 (A46 이연) | UX | Medium |
| 소멸 자동 전이 + 퇴교 보강 처리 + 캘린더 뷰 | Sprint 10 본 마일스톤 | — |

---

## 이전 리스크 현황 참조

- `docs/risk-register/2026-05-22.md` — Sprint 8 리스크 (R48~R62)
- R58 (PI-02 분 단위 전환): ✅ T1에서 옵션 A(일 단위) 확정. T3 주석 위치 보존으로 전환 용이성 유지
- R59 (partial failure): ✅ SQLite ACID + `rows_affected()=1` 검증으로 해소
- R60 (3-way JOIN 복잡도): ✅ Session #10 케이스 A/B 분리 설계로 단순화
- R61 (AttendanceGrid 퇴행): ✅ 기존 출결 토글 기능 시각 검증(I/J 라운드)에서 이상 없음 확인
- R62 (cancel/absent 후 compute_summary 불일치): ✅ T4 + T9 통합 검증에서 확인
