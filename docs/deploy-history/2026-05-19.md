# 배포/검증 기록 — 2026-05-19

---

## 2026-05-19 | v0.1.0 | Sprint 1 — 데이터 인프라

### 포함 스프린트
- Sprint 1: Tauri 셸 + SQLCipher + app.lock + 4계층 백업 + 무결성 검증 + 앱 시작 시퀀스

### 배포 상태
- ✅ main merge 완료
- ✅ v0.1.0 태그 push → GitHub Actions 빌드 트리거
- ⬜ GitHub Release 아티팩트 업로드 확인
  - ⬜ Windows: .msi 또는 .exe
  - ⬜ macOS: .dmg

### Sprint 1 스테이징 검증 (develop 머지 후)
- ✅ sprint-review 에이전트 실행 (코드 리뷰 + 자동 검증 + 회고)
- ⬜ `pnpm tauri:dev` 로 로컬 스테이징 실행 및 주요 흐름 동작 확인
- ⬜ `sqlx migrate run` — V001/V008 마이그레이션 적용 확인
- ⬜ 첫 비밀번호 설정 화면 진입 + `set_password` IPC 정상 동작 확인
- ⬜ 복구 코드 발급 (`recovery_code` 12자리 31자 알파벳) 확인
- ⬜ 앱 재시작 후 `unlock_db` IPC로 잠금 해제 확인
- ⬜ 클라우드 동기화 폴더에 `app.lock` 파일 생성 확인
- ⬜ 앱 종료 후 `backup/exit/` 디렉토리에 백업 파일 생성 확인
- ⬜ 앱 시작 시 `PRAGMA quick_check` 통과 + audit 로그 기록 확인
- ⬜ 앱 시작 ~ 메인 화면 진입 시간 < 3초 측정 (PRD §5.6, cipher off 기준)

### CV — 아티팩트 검증
- ⬜ Release 파일 다운로드 가능 여부 확인
- ⬜ 인스톨러 파일 크기 sanity check (Windows .msi ~50MB+, macOS .dmg ~30MB+ 예상)
- ⬜ cipher 빌드 성공 여부 (GitHub Actions 로그 확인)

### Notion 업데이트
- ⬜ 데이터 모델 — V001/V008 마이그레이션 스키마 반영
- ⬜ 시스템 아키텍처 — SQLCipher/app.lock/4계층 백업/무결성 검증 추가

### 알려진 Risk (Sprint 2 Backlog)
- R6: KEYRING_USER_SALT Keychain 저장 — 멀티디바이스 salt 이전 필요 (Sprint 2 초기 설정 마법사)
- R7: release_lock advisory lock 미적용 — 단일 사용자 모델로 수용, 후속 Sprint 개선
- R8: cipher on 실측 미수행 — Windows + SQLCipher on 환경 첫 실행 시 < 3초 측정 필요
