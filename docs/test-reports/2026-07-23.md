# Test Report - 2026-07-23 (Sprint 23)

> 대상: Sprint 23 — 2026-07-22 프로덕션 데이터 소실 사고 재발방지 (v1.5.0)
> 실행 환경: Windows 11 (개발 PC), cipher-off 기본 빌드 + cipher-on 스모크

---

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| `cargo test` | ✅ 478 passed, 0 failed | cipher off, 4 ignored (doc-test) |
| `cargo clippy --all-targets -D warnings` | ✅ 통과 | 경고 없음 |
| `cargo check --features cipher` | ✅ 통과 | T9 통합 검증 시 확인 |
| `pnpm tsc --noEmit` | ✅ 통과 | 타입 오류 없음 |
| `pnpm lint` | ✅ 통과 | ESLint 경고/오류 없음 |
| `pnpm build` | ✅ 통과 | Next.js static export 성공 |

### 주요 신규 테스트 (Sprint 23 추가분)

| 테스트 | 모듈 | 검증 내용 |
|--------|------|----------|
| `after_connect_reapplies_pragmas_on_reconnect` | `db` | max_lifetime 만료 후 재연결 시 PRAGMA 재적용 (T1/C3) |
| `create_if_missing_guard_blocks_when_setup_done` | `db` | setup_completed=true 시 DB 생성 차단 (T2/C1) |
| `empty_db_reconnect_returns_error` | `db` | 빈 DB fail-hard 동작 |
| `pool_closes_on_idle_and_reconnects` | `db` | close_for_idle 후 pool().await 재연결 (T6/A1) |
| `set_password_rejects_when_salt_exists` | `auth` | 기존 salt 있을 때 set_password 거부 (T5/M2) |
| `try_adopt_key_rejects_invalid_pin` | `auth` | 잘못된 형식 PIN 조기 거부 (T7/B1) |
| `is_drastic_shrink_detects_half_or_less` | `backup` | 50% 미만 축소 감지 로직 (T4/H2) |
| `rotation_never_deletes_below_one` | `backup` | max=0 이어도 최소 1개 보존 (T4/H2) |
| `load_or_create_device_id_at_marks_lost_on_corrupt` | `lock` | 손상 device.id 유실 플래그 (T8/M3) |
| `touch_lock_throttles_mtime_updates` | `lock` | touch_lock 스로틀 동작 (T8/M4) |
| `init_from_config_caches_setup_completed_flag` | `paths` | setup_completed 캐시 초기화 (T5/M1) |
| `init_from_config_infers_setup_done_from_salt_when_flag_missing` | `paths` | salt.bin 존재로 setup_completed 추론 (T5/M1) |

---

## 수동 검증 항목

- ⬜ `pnpm tauri:dev` 실행 후 앱 동작 확인 (개발자 수행 필요)
  - 유휴 5분 후 DB close + 재활동 시 재연결 정상 동작
  - try_adopt_key 흐름 (2번째 PC 환경)
  - create_if_missing 가드 동작 (salt.bin 있고 app.db 없을 때)

---

## 코드 리뷰 주요 발견

- Critical/High: 0건
- Medium: 2건 (M-01: pool TOCTOU, M-02: 오류 분기 문자열 매칭)
- Low: 3건 (eprintln 관찰 가능성, warn_if_drastic_shrink 비차단, A113 상수 확인됨)

---

## 결론

자동 검증 6항목 전부 통과. Sprint 23 신규 단위 테스트 12건 이상 추가(478 → 478, 기존 T9 포함 산정). 수동 검증(유휴 재연결, 2번째 PC 키 채택)은 tauri:dev 실행 후 개발자가 확인 필요.
