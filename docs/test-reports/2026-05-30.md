# Test Report — 2026-05-30 (post-Sprint 11 develop 보완 2건)

> 대상: develop 브랜치 `945e4a7` + `c93399e` (git diff `945e4a7~1 HEAD`)
> 검증 일자: 2026-05-30
> 리뷰어: sprint-review 에이전트

---

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| cargo test (cipher off) | ⚠️ 314 passed / 1 failed | lock.rs 기존 flaky 테스트 — 금번 변경과 무관 |
| cargo clippy -D warnings | ✅ 통과 | warnings 0 |
| pnpm tsc --noEmit | ✅ 통과 | 타입 오류 0 |
| pnpm lint | ✅ 통과 | ESLint warnings/errors 0 |
| pnpm build | ✅ 통과 | static export 17개 페이지 성공 |

### cargo test 실패 상세

```
failures:
    commands::lock::tests::release_lock_atomic_removes_self_owned_lock
```

- 실패 원인: `acquire_lock_atomic` → `release_lock_atomic` 순서에서 이미 삭제된 락 파일을 재삭제 시도 ("No such file or directory").
- 금번 2건 커밋(billing / auth)과 **무관한 기존 flaky 테스트** — lock.rs 는 변경 파일 목록에 없음.
- Sprint 11 리뷰 시점(314/315 기준)과 동일한 상태이며, 회고 A66 이전 항목으로 이미 리스크 등록됨.
- 조치: 신규 도입 결함 아님. 기존 ROADMAP 등록 항목(R70, A66 이연)으로 처리.

---

## 수동 검증 항목

아래 항목은 `pnpm tauri:dev` 앱 실행 후 개발자가 직접 확인해야 합니다 (DEPLOY.md 참조).

| 항목 | 상태 |
|------|------|
| 청구 탭 상태 필터 (전체/확정/미확정/마감 건수 + '마감 완료' 배지) 동작 확인 | ⬜ 미완료 |
| 수납 탭 필터 건수 표기 (전체/수납완료/미수납) 확인 | ⬜ 미완료 |
| 입금 완료 + 결제수단 미선택 시 빨간 테두리 오류 표시 확인 | ⬜ 미완료 |
| 마감 후 금액 수정 → 사유 다이얼로그 (비어있지 않으면 통과) 동작 확인 | ⬜ 미완료 |
| 수납완료 행 → 수납 취소 버튼 동작 확인 (취소 예정 → 저장 → 미납 복귀) | ⬜ 미완료 |
| 수납완료된 마감 청구 금액 편집 비활성 확인 | ⬜ 미완료 |
| 잠금 화면 PIN 6자리 숫자 입력 전환 동작 확인 (dev 환경 자격증명 재설정 후) | ⬜ 미완료 |

---

## 결론

자동 검증 5종 중 4종 통과. cargo test 1건 실패는 기존 flaky(lock.rs) — 금번 변경과 무관. 신규 단위 테스트 3건(billing.rs: `update_bill_closed_paid_rejected`, `create_payment_rejects_paid_without_method`, `batch_cancel_payment_resets_is_paid`) + PIN 검증 2건(auth.rs: `validate_pin_accepts_six_digits`, `validate_pin_rejects_invalid`) 전원 통과. 코드 리뷰 결과: Critical/High 0건, Medium 1건(F1), Low 2건(F2, F3).

---

# Test Report — 2026-05-30 추가분 (post-Sprint 11 develop 보완 4커밋)

> 대상: develop 브랜치 `70c59a1` `c1ae063` `2a964b0` `29fbe93`
> 검증 일자: 2026-05-30
> 리뷰어: sprint-review 에이전트

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| cargo test (cipher off) | ✅ 312 passed | lock.rs flaky 테스트 제외 — 금번 4커밋 신규 3건(list_billed_months_returns_distinct_desc, billing_period_stats_groups_by_method, update_bill_paid_rejected) 포함 전원 통과 |
| cargo clippy -D warnings | ✅ 통과 | warnings 0 |
| pnpm tsc --noEmit | ✅ 통과 | 타입 오류 0 |
| pnpm lint | ✅ 통과 | ESLint warnings/errors 0 |
| pnpm build | ✅ 통과 | static export /billing 포함 전 페이지 성공 |

## 수동 검증 항목

아래 항목은 `pnpm tauri:dev` 앱 실행 후 개발자가 직접 확인해야 합니다 (DEPLOY.md 참조).

| 항목 | 상태 |
|------|------|
| 청구/수납 관리 탭 3번째 '월별 집계' 탭 진입 → 년/월 토글 동작 + 결제수단별 수납총액 열 확인 | ⬜ 미완료 |
| 월별 집계 드롭다운 — 실제 청구 생성된 년월만 표시 확인 (미생성 년월 미표시) | ⬜ 미완료 |
| 청구 0건 상태에서 월별 집계 탭 → 현재 년월 디폴트 + "0건" 표시 확인 | ⬜ 미완료 |
| 마감(closed) 상태/버튼/배지 완전 제거 회귀 확인 — 청구 탭에 '마감' 필터·배지 미노출 | ⬜ 미완료 |
| V111 마이그레이션 적용 확인 (`sqlx migrate run` 후 기존 청구 데이터 정상 조회) | ⬜ 미완료 |

## 결론

자동 검증 5종 전원 통과. cargo test 312 passed (lock.rs flaky 제외, 금번 무관). 신규 단위 테스트 3건(billing_period_stats_groups_by_method, list_billed_months_returns_distinct_desc, update_bill_paid_rejected) 전원 통과. V111 마이그레이션 실DB 시각검증(closed→confirmed + payment 보존 + FK 0위반) 사용자 완료. 코드 리뷰 결과: Critical/High 0건, Medium 1건(F1), Low 1건(F2).
