# Test Report - 2026-06-02 (Sprint 12)

> 대상: Sprint 12 공지문 이미지 생성 + PIN UI 통일 + 복구 코드 제거 + 메뉴 정비
> 브랜치: sprint12 / 기준 커밋: 5e480a0

## 자동 검증 결과

| 항목 | 결과 | 상세 |
|------|------|------|
| `cargo test` | ✅ 통과 | 312 passed / 0 failed / 4 ignored (keychain 부수효과 방지용 ignored 정상) |
| `cargo clippy -- -D warnings` | ✅ 통과 | 경고 없음 |
| `pnpm tsc --noEmit` | ✅ 통과 | 타입 오류 없음 |
| `pnpm lint` | ✅ 통과 | ESLint 경고/오류 없음 |
| `pnpm build` | ✅ 통과 | 19개 정적 페이지 생성 완료 (`/notices` 23.1 kB 포함) |

### 신규 테스트 (Sprint 12 — notice.rs 4건, paths.rs 2건)

| 테스트 이름 | 검증 내용 |
|------------|----------|
| `notice::sanitize_blocks_traversal_and_separators` | `sanitize_component` 경로 traversal(`../`) + 구분자 차단 |
| `notice::save_list_delete_asset_roundtrip` | 배경서식 CRUD 멱등성 + 확장자 거부(`.exe`) |
| `notice::save_notice_image_writes_expected_path` | 공백 제거·YYMM 압축 경로 조합 + `check_notice_output_exists` 격리 |
| `notice::batch_save_returns_count` | 일괄 저장 건수 반환 |
| `paths::assets_dir_composes_under_data_root` | `assets_dir()` 경로 조합 |
| `paths::output_root_composes_under_data_root` | `output_root()` 경로 조합 |

## 수동 검증 항목

- 사용자가 직접 완료한 항목 (사용자 보고 기준):
  - ✅ 공지문 화면 전체 흐름 동작 확인 (배경서식 업로드, 텍스트박스 드래그/리사이즈, 원생 체크박스 선택, 이미지 생성)
  - ✅ PIN UI 6박스(OTP) 동작 확인 (잠금 화면 + PIN 변경 설정 화면)

- 개발자 수행 필요 (DEPLOY.md 연동):
  - ⬜ `pnpm tauri:dev` 실행 후 앱 동작 확인 (스테이징 검증)
  - ⬜ sprint12 → develop 직접 머지

## 결론

전체 자동 검증 통과. 신규 notice 도메인 6건 + 기존 306건 전부 green. clippy + lint + tsc + build 무결.
Medium 등급 이슈 1건(F1: `save_notice_preview` 임의 경로 검증 부재)은 Tauri Dialog 신뢰 경계 내에서 수용 가능 수준으로 판단 — risk-register에 등록.
