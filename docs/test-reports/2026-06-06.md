# Test Report - 2026-06-06 (Sprint 14)

> 대상: Sprint 14 (develop...sprint14) — 대시보드 위젯 7종 + 데이터 자가 진단 + 엑셀 내보내기 + 복원 리허설 + 원생 생년월일 + V303~V305 마이그레이션

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| `cargo test --lib` | 통과 (369 passed, 0 failed, 4 ignored) | cipher off 빌드. 계획(368) 대비 1건 증가 (auto_needed 트래킹 케이스 추가) |
| `cargo clippy -- -D warnings` | 통과 | 경고 0건 |
| `cargo check --features cipher` | 통과 | cipher on 빌드 컴파일 검증 |
| `pnpm lint` | 통과 | ESLint 경고/오류 0건 |
| `pnpm tsc --noEmit` | 통과 | TypeScript strict 오류 0건 |
| `pnpm build` | 통과 | Next.js static export 성공. 라우트: `/`, `/settings/backup`, `/settings/data`, `/settings/diagnosis` 등 14개 |

### 빌드 신규 라우트 확인

| 라우트 | 생성 크기 |
|--------|----------|
| `/` (대시보드) | index.html |
| `/settings/backup` | 2.5 kB |
| `/settings/data` | 1.99 kB |
| `/settings/diagnosis` | 2.67 kB |

## 수동 검증 항목

| 항목 | 상태 | 비고 |
|------|------|------|
| `pnpm tauri:dev` 실행 후 대시보드 위젯 동작 확인 | ✅ 완료 (세션 #3, 2026-06-04) | 위젯 6종·차트·알림 이동·출결 진행률 제거·메모 자동저장 통과 |
| 자가 진단 수동 실행 + 결과 표시 확인 | ✅ 완료 (세션 #3, 2026-06-04) | 수동 실행 A/B/C/D 케이스 통과. 오탐(성춘향) 수정 후 재확인 |
| 내보내기(.xlsx) 3종 파일 생성 확인 | ⬜ 미완료 (개발자 수행 필요) | `pnpm tauri:dev` 실행 후 /settings/data에서 저장 다이얼로그 + 파일 생성 + Excel 열기 |
| 복원 리허설 실행 + 결과 표시 확인 | ⬜ 미완료 (개발자 수행 필요) | cipher off 개발빌드는 평문 SQLite 파일을 backup 디렉토리에 배치해야 리허설 가능 |
| V303~V305 마이그레이션 적용 확인 | ⬜ 미완료 (개발자 수행 필요) | `sqlx migrate run` 후 동작 확인 |

## .sqlx 오프라인 캐시

런타임 `query()` 패턴만 사용하므로 갱신 불필요. CI에서 `SQLX_OFFLINE=true` 통과.

## 결론

자동 검증 전수 통과. cargo test 369건(계획 368건 대비 +1), clippy/lint/tsc/build 모두 clean. 수동 검증 3항목(내보내기·복원 리허설·마이그레이션)은 tauri:dev 환경에서 개발자 확인 필요.
