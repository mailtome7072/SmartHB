# Test Report — 2026-06-12 (Sprint 16 · v1.0.0)

> 검증 범위: Sprint 16 전체 변경분 + sprint-review 코드리뷰 Confirmed 2건 수정 후 재검증
> 검증 시점: T11 통합검증(sprint16 브랜치) + 수정 커밋 9b6034f → develop 머지 e9ebfeb
> 기준 커밋: e9ebfeb (develop HEAD)

---

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| `cargo test --manifest-path src-tauri/Cargo.toml` | ✅ **417 passed / 0 failed** | T11: 415 passed → 수정 후 insert_student_tx 원자성 테스트 2건 추가 → 417 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | ✅ **clean** | 테스트 코드 포함 전수. A98 액션 반영(--all-targets 기준) |
| `cargo check --manifest-path src-tauri/Cargo.toml --features cipher` | ✅ **통과** | SQLCipher 빌드 타입 오류 없음 |
| `pnpm tsc --noEmit` | ✅ **clean** | sprint-review 수정은 backend only, 프론트 무변경 |
| `pnpm lint` | ✅ **clean** | 동일 이유로 재실행 생략(T11 결과 그대로 유효) |
| `pnpm build` | ✅ **성공** | Next.js static export (out/) 생성 확인 |
| 마이그레이션 self-check | ✅ | V301~V307 적용 완료. V306(regular_attendances.note) + V307(regular_attendances.start_time) 신규 |

---

## sprint-review 코드리뷰 수정 검증

### A6: change_data_folder pool 종료 (CONFIRMED → 수정 완료)

- **수정 내용**: `setup.rs`의 `change_data_folder_impl` 내 `write_status` 완료 후 `pool().close()` 추가
- **의도**: OnceCell로 교체 불가한 global pool을 닫아, 재시작 전 후속 IPC의 구 경로 DB 쓰기를 차단
- **검증**: `cargo test` 통과 확인 (기존 lock 테스트 포함)

### C2: CSV import 원자성 (CONFIRMED → 수정 완료)

- **수정 내용**: `import.rs`의 `import_students_csv`를 단일 트랜잭션으로 묶음. `students.rs`에 `insert_student_tx` 헬퍼 추출하여 단건/일괄 공유
- **의도**: CSV 중간 행 실패 시 전체 롤백하여 부분 삽입 상태 방지
- **검증**: 원자성 단위 테스트 2건 추가 후 417 passed 확인
  - `test_import_csv_atomicity_rollback_on_error`: DB 오류 시 전체 롤백 확인
  - `test_import_csv_atomicity_success_all_or_nothing`: 전체 성공 시 전건 삽입 확인

---

## 수동 검증 항목

| 항목 | 상태 | 비고 |
|------|------|------|
| `pnpm tauri:dev` 앱 동작 수동 확인 | ⬜ 미완료 | 사용자 시각검수로 전 태스크 완료됨. deploy-prod 인스톨러 설치 후 최종 확인 |
| cipher 실동작 스모크 테스트(백업/복원/무결성/DB폴더변경) | ⬜ 미완료 | dev 환경은 stub — 첫 실검증은 v1.0.0 인스톨러(.exe) 설치 후 수행 필요 (A101) |
| T4 양 OS 빌드 검증 (.dmg / .exe 설치·실행) | ⬜ 미완료 | 물리 환경 의존. deploy-prod GitHub Actions 빌드 완료 후 수행 |
| T5 양 PC 동기화 시나리오 (Win→Mac, Mac→Win) | ⬜ 미완료 | 물리 환경 의존. 인스톨러 설치 후 수행 |
| sqlx migrate run (개발 DB 적용) | ✅ | V306·V307 적용 완료 |
| UI 시각적 품질 확인 | ✅ | 사용자 시각검수 완료 (전 태스크) |

---

## 알려진 리스크 및 잠재 flake

### 잠재적 Flaky 테스트: `release_lock_atomic_is_idempotent_when_no_file`

- **증상**: T11 및 sprint-review 재실행에서 재현 안 됨
- **원인**: 전역 락 경로(`app.lock`)를 테스트 간 공유. `cargo test` 병렬 실행 시 다른 테스트가 동일 경로 접근하면 간헐 실패 가능
- **현재 대응**: 정상 단일 실행에서 417 passed. 배포 차단 미적용
- **향후 대응**: A102 — tempdir() 또는 #[serial] crate으로 직렬화 검토 (차기 스프린트)

### cipher 실동작 미검증 (배포 후 스모크 테스트 필요)

- `cargo check --features cipher` 타입 레벨 통과는 확인됨
- 백업 API(rusqlite::backup), 무결성(PRAGMA integrity_check), DB 폴더 변경의 cipher 경로는 dev stub — 실행 레벨 검증 미수행
- **조치**: deploy-prod 완료 직후 A101 스모크 테스트 수행 (High 우선순위)

---

## 결론

자동 검증 전 항목 통과. sprint-review 코드리뷰 Confirmed 2건은 당일 수정·원자성 테스트 추가 후 재검증 완료. 배포 게이트(DEPLOY.md ✅ sprint-review) 충족.

수동 항목 3건(cipher 스모크, T4 빌드, T5 동기화)은 deploy-prod 인스톨러 설치 단계에서 수행 예정.
