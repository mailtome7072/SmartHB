# Test Report - 2026-07-08 (Sprint 19)

## 개요

- 대상 브랜치: `sprint19` (develop 대비 70개 커밋)
- 검증 일자: 2026-07-08
- 자동 검증 실행자: sprint-review 에이전트

---

## 자동 검증 결과

| 항목 | 결과 | 상세 |
|------|------|------|
| `cargo test` | ✅ 통과 | 431 passed, 0 failed, 4 ignored (keychain 부수효과 방지) |
| `cargo clippy --all-targets -D warnings` | ✅ 통과 | 경고 없음 |
| `pnpm tsc --noEmit` | ✅ 통과 | 타입 오류 없음 |
| `pnpm lint` | ✅ 통과 | No ESLint warnings or errors |
| `pnpm build` (static export) | ✅ 통과 | 26개 정적 페이지 성공 (신규: /academic/print, /students/roster-print) |

### cargo test 상세

Sprint 18 기준 414건 → Sprint 19에서 431건으로 17건 증가:

- `students::tests::check_and_promote_grade_excluded_for_2026` — 2026년 자동 승급 제외 검증
- `students::tests::check_grade_promotion_needed_false_when_no_eligible_students` — 대상 없을 때 needed=false
- `students::tests::check_grade_promotion_skips_already_processed_year` — 연도 중복 방지
- `students::tests::promote_grades_excludes_max_grade` — 초6/중3 미승급
- `students::tests::promote_grades_excludes_withdrawn_students` — 퇴교생 제외
- `students::tests::promote_grades_increments_elementary_and_middle` — 정상 승급
- `students::tests::gender_and_weekly_hours_sort_have_name_tiebreak` — 정렬 2차 키
- `students::tests::grade_asc_sorts_school_level_then_grade_then_name` — 기본 정렬
- `codes::tests::create_school_stores_school_type_from_extra` — school_type 저장
- `codes::tests::update_school_changes_school_type` — school_type 수정
- `codes::tests::update_school_keeps_school_type_when_extra_is_none` — extra=None 시 유지
- `codes::tests::list_schools_exposes_school_type_as_extra` — school_type 노출
- `attendance::tests::grid_orders_students_by_school_level_grade_then_name` — 출결 그리드 정렬
- 기타 추가 테스트

### 특별 검증 — on_window_event 핸들러 (사용자 요청)

`src-tauri/src/lib.rs` 라인 193-202 확인:

```rust
if let tauri::WindowEvent::CloseRequested { .. } = event {
    if window.label() == "main" {
        for (label, w) in window.webview_windows() {
            if label != "main" {
                let _ = w.close();
            }
        }
        tauri::async_runtime::block_on(startup::exit_hook());
    }
}
```

- academic-print, roster-print 창의 CloseRequested 이벤트에서 exit_hook() 미실행 — 검증 완료 ✅
- "main" 창 닫힐 때만 (1) 보조 창 모두 닫기 → (2) exit_hook() 실행 — 로직 올바름 ✅
- 인쇄 창 닫기 시 DB pool 종료 버그 재발 없음 확인 ✅

---

## 코드 리뷰 요약

검토 범위: 변경 파일 69개 (백엔드 10개 + 프론트엔드 46개 + 문서 13개)

| 등급 | 건수 |
|------|------|
| Critical | 0 |
| High | 1 (GradePromotionDialog.tsx promoteGrades 에러 catch 누락) |
| Medium | 1 (useTableSort.ts desc 방향 tiebreak 역전) |
| Low | 2 (students.rs EnrollDate tiebreak 불일치, attendance.rs school_level 미검증) |

상세: `docs/code-reviews/sprint19.md`

**보안 체크 결과**:
- `dangerouslySetInnerHTML` 사용: 없음 ✅
- `invoke()` 직접 호출: 없음 ✅
- `localStorage` 민감 정보 저장: 없음 (UI 상태 + 원생 폼 임시저장만, PRD §5.7 요구사항) ✅
- SQL 인젝션 패턴: 없음 (bind() 파라미터 일관 사용) ✅
- 하드코딩 시크릿: 없음 ✅
- `unwrap()` 프로덕션 코드 사용: 없음 (테스트 코드에서만 사용) ✅
- Tauri capabilities 최소 권한: 준수 ✅

**인쇄 아키텍처 XSS 검증**:
- `student-roster-print-html.ts`: `escapeHtml()` 함수로 모든 사용자 데이터 escape 처리 ✅
- `academic-print-html.ts`: 동일 패턴, 사용자 입력을 HTML 속성/내용에 직접 삽입하지 않음 ✅

---

## 수동 검증 항목

아래 항목은 자동화할 수 없어 개발자 육안 확인이 필요합니다:

- ⬜ `pnpm tauri:dev` 실행 후 앱 동작 수동 확인
- ⬜ 원생 목록 기본 정렬 — 학년+이름순 표시 확인
- ⬜ 출결/청구 그리드 컬럼 클릭 정렬 동작 확인
- ⬜ 출결 그리드 가로 스크롤바 즉시 접근 가능 확인
- ⬜ 교습일정 인쇄 — Red 테두리 + 기간성 밴드 + 폰트 확대 + 동적 행 수 (인쇄 미리보기)
- ⬜ 수업관리 주보기 — 이름만 표시, 2xN 규칙 준수 확인
- ⬜ 수업관리 일보기 — 10명 초과 시 10xN 규칙 분할 확인
- ⬜ 캘린더 라인 진한 테두리 확인
- ⬜ 대시보드 상하 배치 + 2:1 비율 확인
- ⬜ 학교급 필터 — 설정 > 학교에 school_type 선택 UI, 원생 폼에서 필터 동작 확인
- ⬜ 수강생대장 출력 — 인쇄 미리보기 및 매 페이지 반복 헤더 확인
- ⬜ `sqlx migrate run` 실행 시 V310 정상 적용 확인

---

## 결론

자동 검증 5종 전체 통과. High 1건(GradePromotionDialog 에러 catch 누락) 발견 — 다음 스프린트 T0 또는 즉시 수정 권고. Medium 1건, Low 2건은 risk-register 등록. 수동 검증 항목은 개발자 확인 후 스테이징 완료 처리.
