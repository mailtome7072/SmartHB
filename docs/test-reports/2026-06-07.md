# Test Report - 2026-06-07 (Sprint 15)

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| `cargo test` | 통과 | 375 passed, 0 failed, 4 ignored (cipher off 모드) |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | 통과 | 경고 없음 (테스트 코드 포함 전수 검사) |
| `cargo check --features cipher` | 통과 | vendored OpenSSL 컴파일 정상 (macOS) |
| `pnpm lint` | 통과 | No ESLint warnings or errors |
| `pnpm tsc --noEmit` | 통과 | 타입 오류 없음 |
| `pnpm build` | 통과 | Static export 성공 (`out/` 생성) |

## 수동 검증 항목

- `pnpm tauri:dev` 실행 후 앱 동작 확인: ⬜ 미완료 (개발자 수행 필요)
  - 교습소 정보 화면(`/settings/info`) — 텍스트 저장, 이미지 업로드/미리보기/삭제
  - 자가 진단 이력 삭제 — 행 단위 삭제, 전체 비우기, 확인 모달
  - GlobalShortcuts — Ctrl+F(검색 포커스), Ctrl+N(신규 원생 이동)
  - GlobalTooltip — title 요소 hover 시 20px 커스텀 팝업 표시
  - 대시보드 월 핵심 요약 — 청구/수납 집계 정상 표시
- 양 OS 인스톨러(.dmg/.msi) 설치·실행 검증: ⬜ Sprint 16 이연 (물리 환경 의존)

## 결론

자동 검증 6종 전수 통과. Critical/High 코드 리뷰 이슈 없음. Medium 1건(미저장 이탈 경고, Sprint 16 이연) 확인. 수동 검증 및 양 OS 빌드 검증은 개발자 수행 필요.
