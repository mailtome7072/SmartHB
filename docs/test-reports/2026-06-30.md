# Test Report - 2026-06-30 (Sprint 17)

> 대상: Sprint 17 (82c385a) — DB 안전성 잔여 수정 + 정책 간소화
> 작성일: 2026-06-30

## 자동 검증 결과

| 항목 | 결과 | 비고 |
|------|------|------|
| `cargo test --lib` | 통과 | 411 passed |
| `cargo clippy --all-targets -- -D warnings` | 통과 | 경고 없음 |
| `cargo check --features cipher` | 통과 | SQLCipher 빌드 확인 |
| `pnpm lint` | 통과 | ESLint 경고/오류 없음 |
| `pnpm tsc --noEmit` | 통과 | TypeScript 타입 오류 없음 |
| `pnpm build` | 통과 | Next.js static export 성공 |

## 수동 검증 항목

- `pnpm tauri:dev` 실행 후 앱 동작 확인: ⬜ 미완료 (개발자 수행 필요)
  - 앱 시작 시 DB pool 초기화 및 integrity_check 정상 통과 확인
  - 백업 디렉토리 tmp 파일 정리 동작 확인
  - SyncStatus UI 상단바 표시 제거 확인 (동기화 표시 없어야 함)
  - DB 폴더 변경 시 WAL 체크포인트 오류 메시지 표시 확인

## 코드 리뷰 발견 사항 (별도 문서)

상세 내용: `docs/code-reviews/sprint17.md` 참조

- High: 2건 (stale lock 취약점, rollback 파일명 충돌)
- Medium: 3건 (WAL pool.close 미실행, spawn_blocking 누락, 단위 테스트 누락)
- Low: 2건 (오류 메시지 오해 소지, last_err 초기화 패턴)

## 결론

자동 검증 6개 항목 전체 통과. 코드 리뷰에서 High 2건 발견 — 특히 stale lock 취약점(T5)은
heartbeat 제거의 부작용으로 5분 이상 사용 시 두 번째 PC가 락을 자동 점유할 수 있는 구조적 문제.
수동 검증(pnpm tauri:dev)은 개발자 실행 필요.
