{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SmartHB 기본 권한",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
